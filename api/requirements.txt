# Web API — install from repo root with PYTHONPATH set.
# Reuses core/ (SQLAlchemy, NetworkX, pandas). Live needs psutil.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic>=2.0.0
pydantic-settings>=2.0.0
SQLAlchemy>=2.0.0
networkx>=3.0
pandas>=2.0.0
psutil>=5.9.0

# Optional (EVTX via core report_parser):
# python-evtx
